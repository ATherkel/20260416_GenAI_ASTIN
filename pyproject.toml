[project]
name = "genai-astin-demo"
version = "0.1.0"
description = "GenAI ASTIN demo repository"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
]
